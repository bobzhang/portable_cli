///|
/// Entry point that points users at the portable CLI examples.
/// Run individual commands with `moon run --target wasm cmd/<name>`.
fn main {
  println("portable_cli commands: cmd/cow, cmd/jqlet, cmd/tree, cmd/pulse")
}
